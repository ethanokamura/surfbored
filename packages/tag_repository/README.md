# Tag Repository

```
tag_repository/lib/src/
  ├── tag_repository.dart         # Handles requests and responses to the database
  ├── failures.dart               # Handles failures when performing operations
  └── models/models.dart          # Handles custom data types for tags
```