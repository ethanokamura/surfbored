name: surfbored
description: "create and share activities"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  algolia_helper_flutter: ^1.0.0  # remove!
  api_client:
    path: packages/api_client
  app_core:
    path: packages/app_core
  app_ui:
    path: packages/app_ui
  board_repository:
    path: packages/board_repository
  comment_repository:
    path: packages/comment_repository
  flow_builder: ^0.1.0  # move! also make use of this
  flutter:
    sdk: flutter
  friend_repository:
    path: packages/friend_repository
  infinite_scroll_pagination: ^4.0.0  # remove? or keep for later
  intl: ^0.19.0  # needed for what?
  post_repository:
    path: packages/post_repository
  shared_preferences: ^2.0.6  # this is for theme. maybe delete?
  sqflite: ^2.0.0+3  # needed for what?
  tag_repository:
    path: packages/tag_repository
  user_repository:
    path: packages/user_repository

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - .env
