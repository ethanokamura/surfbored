import 'package:flutter/material.dart';

/// TODO
/// Grab a future of the list of boards,
/// have a check list to see if the item is inside the board
/// add or remove item from boards

class AddToBoardScreen extends StatelessWidget {
  final String userID;
  const AddToBoardScreen({super.key, required this.userID});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
