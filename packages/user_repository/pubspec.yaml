name: user_repository
description: User repository.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  api_client:
    path: ../api_client
  app_core:
    path: ../app_core
  flutter:
    sdk: flutter

dev_dependencies:
  very_good_analysis: ^6.0.0
