# Comment Repository

```
comment_repository/lib/src/
  ├── comment_repository.dart        # Handles requests and responses to the database
  ├── failures.dart                  # Handles failures when performing operations
  └── models/models.dart             # Handles custom data types for the comments
```